# Hooks Customizados

Este diretório agrupa hooks reutilizáveis do projeto. No momento, o sistema utiliza principalmente o `AuthProvider`, mas o espaço está pronto para hooks adicionais que lidem com efeitos, fetches ou assinaturas de eventos.
