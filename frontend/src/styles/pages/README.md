# Page Styles

Os estilos específicos para páginas (ex.: Dashboard, Login, ambiente) devem ser colocados aqui como arquivos CSS separados. Cada arquivo pode ser importado pelo componente correspondente quando houver regras muito específicas que não fazem sentido no `global.css`.
